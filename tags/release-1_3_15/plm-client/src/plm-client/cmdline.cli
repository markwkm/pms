Name plm

Usage {}

Version [exec cat Makefile | grep VERSION\ = | cut -d\  -f3]\n

Commandline tool

Flag -h help {View usage information\n}

Flag -v verbose {Enable verbose program operation}

Flag -d debug {Enable debugging level logging}

Flag -s syslog {Enable logging to the syslog}

Flag -q quiet {Turn off stdout output\n}

String -f filename {File containing patch} 
   
String -a applies {Version(s) of software this patch applies to} \
   -c 1 oo

String -n name {Name of this patch} 

